@model TheFitnessApp_1.Models.DashboardViewModel

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" />
}

<div class="welcome-page">

    <div class="dashboard">

        <h1>Hej @Model.UserName!</h1>

        <!-- Snabbsummering -->
        <div class="summary">

            <div class="summary-item">
                <div class="icon">💪</div>
                <div class="value">@Model.TotalSessions</div>
                <div class="label">Träningspass</div>
            </div>

            <div class="summary-item">
                <div class="icon">🎯</div>
                <div class="value">@Model.ActiveGoals</div>
                <div class="label">Aktiva mål</div>
            </div>

        </div>

        <!-- Total tid -->
        <div class="summary-item">
            <div class="icon">⏱️</div>
            <div class="value">@Model.TotalMinutes min</div>
            <div class="label">Total tid</div>
        </div>

        <!-- Navigering -->
        <div class="actions">
            <a class="btn" asp-controller="Workout" asp-action="AddSession">+ Lägg till träningspass</a>
            <a class="btn" asp-controller="Goals" asp-action="Index">🎯 Sätt mål</a>
            <a class="btn" asp-controller="Statistics" asp-action="Index">📊 Se statistik</a>
        </div>

    </div>

</div>
