@model TheFitnessApp_1.Models.DashboardViewModel

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" />
}

<div class="welcome-page">

    <div class="dashboard">

        <h1>Hi @Model.UserName!</h1>

        <!-- Snabbsummering -->
        <div class="summary">

            <div class="summary-item">
                <div class="icon">💪</div>
                <div class="value">@Model.TotalSessions</div>
                <div class="label">Workout session</div>
            </div>

            <div class="summary-item">
                <div class="icon">🎯</div>
                <div class="value">@Model.ActiveGoals</div>
                <div class="label">Active goal</div>
            </div>

        </div>

        <!-- Total tid -->
        <div class="summary-item">
            <div class="icon">⏱️</div>
            <div class="value">@Model.TotalMinutes min</div>
            <div class="label">Total time</div>
        </div>

        <!-- Navigering -->
        <div class="actions">
            <a class="btn" asp-controller="Workout" asp-action="AddSession">+ Add workout sessions</a>
            <a class="btn" asp-controller="Goals" asp-action="Index">🎯 My goals</a>
            <a class="btn" asp-controller="Statistics" asp-action="Index">📊 Statistic</a>
        </div>

    </div>

</div>
