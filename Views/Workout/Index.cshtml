@{
    ViewData["Title"] = "Workouts";
}

<div class="container mt-5">

    <h1 class="mb-4">Workout Sessions</h1>

    <!-- Temporär text tills data kopplas från backend -->
    <p>Här kommer en lista med träningspass visas.</p>

    <!-- Länk till Create -->
    <a asp-controller="Workout"
       asp-action="Create"
       class="btn btn-primary mt-3">
        Create new workout
    </a>

</div>
